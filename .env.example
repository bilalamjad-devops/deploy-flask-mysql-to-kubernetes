# Copy to .env for local use. Never commit the real .env.
DB_HOST=127.0.0.1 # mysql-service # It is written in mysql-configmap.yaml
DB_USER=root
DB_PASSWORD=mysqlpassword
DB_NAME=web_db
FLASK_DEBUG=false
