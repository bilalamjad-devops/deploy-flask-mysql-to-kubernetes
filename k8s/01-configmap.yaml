apiVersion: v1
kind: ConfigMap
metadata:
  name: flask-config
  namespace: flask-mysql
data:
  DB_HOST: "mysql-service"   # matches the MySQL Service name below -- K8s DNS resolves this automatically
  DB_NAME: "web_db"
  FLASK_DEBUG: "false"
